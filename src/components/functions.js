export const manejarClic = (numClics, setNumClics) => {
    setNumClics(numClics + 1);
};
  
export const manejarClic10 = (numClics, setNumClics) => {
    setNumClics(numClics + 10);
};
  
export const reiniciarContador = (setNumClics) => {
    setNumClics(0);
};
  